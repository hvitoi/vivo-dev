const resFrontend = {
  devTypes: ['frontend']
};

const resBackend = {
  devTypes: ['backend']
};

const resMobile = {
  devTypes: ['mobile']
};

const resGeneric = {
  devTypes: ['generic']
};

const resFrontBack = {
  devTypes: ['frontend', 'backend']
};

const resError = {
  errors: [
    {
      message: 'Houve um problema'
    }
  ]
};

export {
  resFrontend,
  resBackend,
  resMobile,
  resGeneric,
  resFrontBack,
  resError
};
